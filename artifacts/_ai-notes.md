- [user-caught] Concluded pills follow a "deterministic two-path design" (greeting always wins for new users, key written when greeting fires) — missed that the key is written at first mount independent of login state, persists through login/logout, and pills appear on every subsequent visit. Required two rounds of re-investigation (manual contradiction from Osama showing pills post-login/logout + controlled path A/B headed re-test) to find the actual mechanism. Original claim also overstated severity ("pills never render for new users" was wrong; it's a first-visit-only gap, self-correcting on the next visit).
- [user-caught] Reported minLength threshold as 30 in description but 50 in implementation — corrected after Osama flagged the inconsistency.
- [user-caught] Missing comment clarifying waitForTimeout is the poll interval, not the wait mechanism — added after Osama flagged it.
